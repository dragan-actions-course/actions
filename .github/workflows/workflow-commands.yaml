name: workflow-commands
on:
  push:
    branches:
      - main   

jobs:
  testing-workflow-commands:
    runs-on: ubuntu-latest
    steps:
      - name: settings an error message
       
        run: |
          echo "::error::Missing semicolon"
          echo "Listing files in the repository:"
          ls -la
          echo "Workflow commands displayed successfully."
      - name: setting a warning message with params
        run: echo "::error title=Error title,file:app.js,line:10,col:15::Unexpected token"
