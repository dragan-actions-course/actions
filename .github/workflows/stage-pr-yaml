name: Deploy PR branches
on:
  issuse_comment:
    types: [opened, edited, synchronize, reopened]
   
  pull_request_target:
    types: [opened, edited, synchronize, reopened]


jobs:
  trigger-pr-branch-deploy:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - run: echo '${{toJson(github.event)}}'
      - run: echo '${{toJosn(github.ref)}}'